class ShippingAddress {
  var id;
  var name;
  var number;
  var email;
  var country;
  var state;
  var city;
  var landmark;
  var shippingAddress;
  ShippingAddress(this.id, this.name, this.number, this.email, this.country,
      this.state, this.city, this.landmark, this.shippingAddress);
}
